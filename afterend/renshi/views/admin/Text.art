{{extend './common/layout.art'}}

{{block 'main'}}
    {{include './common/header.art'}}
    <!-- 主体内容 -->
    <div class="content">
        {{include './common/aside.art'}}

        <div class="main">
            <!-- 分类标题 -->
        <div class="title">
                <h4>录音上传</h4>
                <span>testing</span>
                <a href="/admin/Text-edit" class="btn btn-primary new">上传录音</a>
            </div>
            <!-- /分类标题 -->
            <!-- 内容列表 -->
            <table class="table table-striped table-bordered table-hover custom-table">
            <thead>
                    <tr>
                        <th>演讲主题</th>
                        <th>演讲者</th>
                        <th>演讲开始时间</th>
                        <th>演讲发布者</th>
                    </tr>
            </thead>
                <tbody>
                     {{each audioText.records}}
                    <tr>
                        <td>{{$value.title}}</td>
                        <td>{{$value.presenter}}</td>
                        <td>{{dateFormat($value.publishDate, 'yyyy-mm-dd HH:mm:ss')}}</td>
                        <td>{{$value.author}}</td>
                        <td>
                            <a href="/admin/Text-edit?id={{@$value._id}}" class="glyphicon glyphicon-edit"></a>
                            <i class="glyphicon glyphicon-remove delete" data-toggle="modal" data-target=".confirm-modal" data-id="{{@$value._id}}"></i>
                            <a href="/admin/Text-show?id={{@$value._id}}" class="glyphicon glyphicon-edit"> 语音转文字</a>
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
            <!-- /内容列表 -->
            <!-- 分页 -->
            <ul class="pagination">
            {{if audioText.page>1}}
                <li>
                    <a href="/admin/Text?page={{audioText.page - 1}}">
                    <span>&laquo;</span>
                </a>
                </li>
            {{/if}}
            
                {{each audioText.display}}
                <li><a href="/admin/Text?page={{$value}}">{{$value}}</a></li>
                {{/each}}
            {{if audioText.page < audioText.pages}}
                <li>
                    <a href="/admin/Text?page={{audioText.page - 0 + 1}}">
                    <span>&raquo;</span>
                </a>
                </li>
            {{/if}}
            
            </ul>
            <!-- /分页 -->
        </div>
    </div>
    <!-- /主体内容 -->
    
    <!-- 删除确认弹出框 -->
    <div class="modal fade confirm-modal">
        <div class="modal-dialog modal-lg">
            <form class="modal-content" action="/admin/delete-Text" method='get'>
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal"><span>&times;</span></button>
                    <h4 class="modal-title">请确认</h4>
                </div>
                <div class="modal-body">
                    <p>您确定要删除吗?</p>
                    <input type="hidden" name="id" id="deleteArticleId">
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">取消</button>
                    <input type="submit" class="btn btn-primary">
                </div>
            </form>
        </div>
    </div>
    <!-- /删除确认弹出框 -->
{{/block}}
{{block 'script'}}
    <script type="text/javascript">
        $('.delete').on('click', function () {
            // 获取用户id
            var id = $(this).attr('data-id');
            // 将要删除的用户id存储在隐藏域中
            $('#deleteArticleId').val(id);
        })
    </script>
{{/block}}
